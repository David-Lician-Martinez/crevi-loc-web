# Copy this file to keystore.properties and replace every placeholder locally.
# Never commit the completed keystore.properties file.
storeFile=release-keystore.jks
storePassword=REPLACE_WITH_PRIVATE_PASSWORD
keyAlias=creviloc
keyPassword=REPLACE_WITH_PRIVATE_PASSWORD
