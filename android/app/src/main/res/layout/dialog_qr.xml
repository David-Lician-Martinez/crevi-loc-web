<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/qrDialogRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="22dp">

    <TextView
        android:id="@+id/qrDialogTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/qr_download_title"
        android:textSize="20sp"
        android:textStyle="bold" />

    <com.alice.partidascrevillente.QrSquareImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="18dp"
        android:adjustViewBounds="true"
        android:background="@drawable/qr_display_background"
        android:clipToOutline="true"
        android:contentDescription="@string/qr_download_title"
        android:maxWidth="320dp"
        android:maxHeight="320dp"
        android:scaleType="fitCenter"
        android:src="@drawable/crevi_loc_web_qr" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="18dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/qrDialogBack"
            android:layout_width="0dp"
            android:layout_height="52dp"
            android:layout_weight="1"
            android:text="@string/qr_dialog_back"
            android:textAllCaps="false" />

        <Button
            android:id="@+id/qrDialogDownload"
            android:layout_width="0dp"
            android:layout_height="52dp"
            android:layout_marginStart="12dp"
            android:layout_weight="1"
            android:background="@drawable/primary_button_bg"
            android:text="@string/qr_dialog_download"
            android:textAllCaps="false"
            android:textColor="#FFFFFF" />
    </LinearLayout>

</LinearLayout>
