# WhatsApp exports

Place WhatsApp exports used as local data sources under one folder per origin or group.

These exports are intentionally ignored by Git. If a future enrichment process extracts public location data from them, commit only the sanitized derived dataset, never the raw chat export.

